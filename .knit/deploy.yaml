version: 1
on:
  push:
    deployments:
      - target: dev
        cwd: /eliware/test8
        timeoutMs: 300000
        commands:
          - git pull --ff-only
          - npm ci
          - npm test
